server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA ルーティング（パス直叩きでも index.html を返す）
  location / {
    try_files $uri /index.html;
  }

  # ヘルスチェック
  location = /healthz {
    add_header Content-Type text/plain;
    return 200 "ok";
  }
}
